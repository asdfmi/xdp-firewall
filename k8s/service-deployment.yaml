apiVersion: apps/v1
kind: Deployment
metadata:
  name: xdp-service
  labels:
    app: xdp-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xdp-service
  template:
    metadata:
      labels:
        app: xdp-service
    spec:
      containers:
        - name: service-health
          image: your-registry/xdp-service:latest
          imagePullPolicy: IfNotPresent
          command: ["/usr/local/bin/service-health"]
          args:
            - "8081"
          ports:
            - containerPort: 8081
              name: http
      terminationGracePeriodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: xdp-service
  labels:
    app: xdp-service
spec:
  selector:
    app: xdp-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP
